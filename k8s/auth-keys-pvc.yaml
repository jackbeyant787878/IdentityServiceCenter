apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-keys-pvc
  namespace: identity
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path   # K3s 默认存储类，请核对
